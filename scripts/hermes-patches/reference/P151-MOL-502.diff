--- a/plugins/memory/tiered/embeddings.py
+++ b/plugins/memory/tiered/embeddings.py
@@ -1,7 +1,12 @@
 """Ollama embedding client for tiered memory.
 
-Generates 1024-dim embeddings via the local Ollama API using BGE-M3.
-Returns None on failure to enable FTS-only fallback.
+Generates 768-dim embeddings via the local Ollama API using nomic-embed-text
+(P151/MOL-502: was bge-m3 @ 1024-dim / 1.3 GB resident; nomic-embed-text is
+274 MB and cold-loads in ~3-5s instead of ~30s). Returns None on failure to
+enable FTS-only fallback.
+
+`keep_alive: 90s` (was Ollama default 5min) — evicts faster between bursts,
+leaves headroom for cron clusters spaced 60-90s apart.
 """
 
 import logging
@@ -12,19 +17,24 @@
 logger = logging.getLogger(__name__)
 
 OLLAMA_URL = "http://localhost:11434/api/embed"
-MODEL = "bge-m3"
+MODEL = "nomic-embed-text"  # P151/MOL-502 — was "bge-m3"
 MAX_CHARS = 8000
-DIMS = 1024
-TIMEOUT = 30.0  # BGE-M3 is 1.2GB — cold-start needs more time
+DIMS = 768                  # P151/MOL-502 — was 1024 (bge-m3); migration re-embeds existing entries
+TIMEOUT = 30.0  # First-call cold-load is ~3-5s; 30s keeps slack for rare slow loads
+KEEP_ALIVE = 90  # seconds; faster eviction than Ollama default 5min
 
 
 def generate_embedding(text: str) -> Optional[list[float]]:
-    """Generate 1024-dim embedding via Ollama. Returns None on failure (FTS-only fallback)."""
+    """Generate 768-dim embedding via Ollama. Returns None on failure (FTS-only fallback)."""
     text = text[:MAX_CHARS].strip()
     if not text:
         return None
     try:
-        resp = httpx.post(OLLAMA_URL, json={"model": MODEL, "input": text}, timeout=TIMEOUT)
+        resp = httpx.post(
+            OLLAMA_URL,
+            json={"model": MODEL, "input": text, "keep_alive": KEEP_ALIVE},
+            timeout=TIMEOUT,
+        )
         resp.raise_for_status()
         data = resp.json()
         emb = data["embeddings"][0]
@@ -48,7 +58,11 @@
         return [None] * len(texts)
     non_empty_texts = [stripped[i] for i in non_empty_indices]
     try:
-        resp = httpx.post(OLLAMA_URL, json={"model": MODEL, "input": non_empty_texts}, timeout=TIMEOUT * 2)
+        resp = httpx.post(
+            OLLAMA_URL,
+            json={"model": MODEL, "input": non_empty_texts, "keep_alive": KEEP_ALIVE},
+            timeout=TIMEOUT * 2,
+        )
         resp.raise_for_status()
         raw_embeddings = resp.json()["embeddings"]
         # Validate dimensions
--- a/plugins/memory/tiered/search.py
+++ b/plugins/memory/tiered/search.py
@@ -1,11 +1,19 @@
 """Hybrid FTS5 + vector search with Reciprocal Rank Fusion.
 
 Ranking formula:
-    score = RRF_base * category_weight * recency_multiplier
+    score = RRF_base * category_weight * recency_multiplier * wing_weight
 
 RRF_base combines BM25 (FTS) and cosine (vector) rankings with tunable weights.
 Category weights favor durable knowledge (principle > project > briefing > chat).
 Recency multiplier gently boosts recent entries without crowding out older signal.
+Wing weights down-rank broad source classes (diary, obsidian) that match widely
+but contribute low specific recall value.
+
+P151/MOL-502: learned reranker (qwen3:8b via /api/generate) removed. MOL-176's
+"+34% recall came from data organization, not algorithm" finding is the design
+license; the reranker was also failing cold (10s call vs >10s qwen3:8b load)
+and silently falling back to RRF order anyway. Hybrid RRF + the four
+weight multipliers above carry the full recall load.
 """
 
 import logging
@@ -13,8 +21,6 @@
 from datetime import datetime, timezone
 from typing import Optional
 
-import httpx
-
 from .embeddings import generate_embedding
 from .store import TieredMemoryDB
 
@@ -50,12 +56,6 @@
     "default": 1.0,   # Project/principle entries — full weight
 }
 
-# Local LLM reranker via Ollama — no cloud API calls
-RERANK_MODEL = "qwen3:8b"
-OLLAMA_GENERATE_URL = "http://localhost:11434/api/generate"
-RERANK_TIMEOUT = 10.0
-
-
 def _parse_created_at(created_at: str) -> Optional[datetime]:
     """Parse SQLite datetime('now') output. Returns None on failure."""
     if not created_at:
@@ -159,62 +159,6 @@
     return [{**entries[eid], "score": score} for eid, score in ranked]
 
 
-_THINK_RE = re.compile(r"<think(?:ing)?[^>]*>.*?</think(?:ing)?>", re.DOTALL | re.IGNORECASE)
-
-
-def _rerank_with_local_llm(query: str, candidates: list[dict], limit: int) -> list[dict]:
-    """Rerank search candidates using local Qwen 3:8b via Ollama generate API.
-
-    Qwen3:8b uses thinking mode (<think> tags) for reasoning before ranking.
-    num_predict budget accommodates thinking tokens + final answer.
-    Falls back to original RRF order on any failure (timeout, parse error, model down).
-    """
-    if len(candidates) <= 1:
-        return candidates
-    numbered = "\n".join(
-        f"[{i}] {c['title']}: {c['content'][:200]}"
-        for i, c in enumerate(candidates)
-    )
-    prompt = (
-        f'I have {len(candidates)} memory entries. Sort by relevance to "{query}".\n\n'
-        f'{numbered}\n\n'
-        'Return just the entry numbers, most relevant first:'
-    )
-    try:
-        resp = httpx.post(
-            OLLAMA_GENERATE_URL,
-            json={
-                "model": RERANK_MODEL,
-                "prompt": prompt,
-                "stream": False,
-                "options": {"temperature": 0.0, "num_predict": 256},
-            },
-            timeout=RERANK_TIMEOUT,
-        )
-        resp.raise_for_status()
-        ranking_text = resp.json().get("response", "").strip()
-        # Strip <think>...</think> blocks so reasoning tokens don't
-        # leak digit sequences into the ranking parser.
-        ranking_text = _THINK_RE.sub("", ranking_text).strip()
-        # Robust parsing: extract ALL digit sequences (handles prose like "1, then 3, then 0")
-        indices = [int(m) for m in re.findall(r'\b(\d+)\b', ranking_text)]
-        indices = [i for i in indices if 0 <= i < len(candidates)]
-        # Reorder: ranked indices first, then any unmentioned in original order
-        seen: set[int] = set()
-        reranked: list[dict] = []
-        for i in indices:
-            if i not in seen:
-                reranked.append(candidates[i])
-                seen.add(i)
-        for i, c in enumerate(candidates):
-            if i not in seen:
-                reranked.append(c)
-        return reranked[:limit]
-    except Exception as e:
-        logger.warning("Local LLM rerank failed, keeping RRF order: %s", e)
-        return candidates[:limit]
-
-
 def hybrid_search(
     db: TieredMemoryDB,
     query: str,
@@ -222,11 +166,16 @@
     category: Optional[str] = None,
     wing: Optional[str] = None,
     room: Optional[str] = None,
-    rerank: bool = True,
     include_superseded: bool = False,
+    **_compat: object,  # P151/MOL-502: swallow legacy `rerank=` from old callers
 ) -> list[dict]:
     """Full hybrid search: FTS5 + sqlite-vec + RRF merge.
-    Falls back to FTS-only if Ollama is unreachable."""
+    Falls back to FTS-only if Ollama is unreachable.
+
+    P151/MOL-502: the optional `rerank=` parameter is gone; the **_compat
+    catch-all preserves backward compatibility for any out-of-tree caller
+    that still passes it.
+    """
     fetch_limit = limit * 4
 
     fts_results = db.search_fts(query, limit=fetch_limit, category=category, wing=wing, room=room, include_superseded=include_superseded)
@@ -235,8 +184,6 @@
     if embedding:
         vec_results = db.search_vec(embedding, limit=fetch_limit, category=category, wing=wing, room=room, include_superseded=include_superseded)
         results = reciprocal_rank_fusion(fts_results, vec_results, limit, query=query)
-        if rerank and results:
-            results = _rerank_with_local_llm(query, results, limit)
         for r in results:
             r["search_mode"] = "hybrid"
         return results
--- a/plugins/memory/tiered/__init__.py
+++ b/plugins/memory/tiered/__init__.py
@@ -197,8 +197,7 @@
                     query,
                     limit=args.get("limit", 5),
                     category=args.get("category"),
-                    rerank=False,
-                )
+                )  # P151/MOL-502: rerank= removed
                 # P13 / MOL-168 item 2: truncate per-entry content before
                 # returning. Without this, large ingested entries (Obsidian,
                 # diary, Granola) balloon the tool output and ultimately
@@ -293,11 +292,14 @@
         room_filter = inferred_room if inferred_room != "general" else None
 
         # Hybrid search with room partitioning
+        # P151/MOL-502: rerank= argument removed; hybrid RRF + wing/room/category/recency
+        # weighting carries the recall load. The learned qwen3:8b reranker was failing
+        # cold (10s timeout < model load time) and silently falling back to RRF anyway.
         try:
-            results = hybrid_search(self._db, query, limit=5, room=room_filter, rerank=True)
+            results = hybrid_search(self._db, query, limit=5, room=room_filter)
             # If room-filtered search returns too few results, fall back to full corpus
             if len(results) < 2 and room_filter:
-                results = hybrid_search(self._db, query, limit=5, rerank=True)
+                results = hybrid_search(self._db, query, limit=5)
         except Exception:
             logger.warning("prefetch failed, returning empty context", exc_info=True)
             results = []
--- a/plugins/memory/tiered/llm.py
+++ b/plugins/memory/tiered/llm.py
@@ -1,6 +1,11 @@
 """Shared LLM helper for tiered memory composition.
 
-Primary: qwen3:8b via local Ollama (thinking on, /think soft-switch).
+Primary: qwen3:1.7b via local Ollama (thinking on, /think soft-switch).
+P151/MOL-502: downsized from qwen3:8b (~8.4 GB resident) to qwen3:1.7b
+(~2.5 GB) for nightly consolidation/composition. Nightly batch text — modest
+quality drop accepted. Composition dry-run gate (composition_review side
+log) wraps the first cron run before in-place rewrites are enabled.
+
 Fallback: moonshotai/kimi-k2.6 via OpenRouter (P61/MOL-268 — cost-control
 flip 2026-04-23; was google/gemini-3.1-pro-preview, flipped after reviewer-
 feedback contagion drove 4am memory cron to 151 tool calls × Gemini pricing).
@@ -16,11 +21,15 @@
 
 logger = logging.getLogger(__name__)
 
-# ── Primary: Ollama qwen3:8b (local, $0) ─────────────────────────────
-PRIMARY_MODEL = "qwen3:8b"
+# ── Primary: Ollama qwen3:1.7b (local, $0) ───────────────────────────
+# P151/MOL-502: was qwen3:8b (8.4 GB resident); qwen3:1.7b is ~2.5 GB and
+# cold-loads ~5× faster. keep_alive=90 (was Ollama default 5min) evicts
+# faster between cron bursts.
+PRIMARY_MODEL = "qwen3:1.7b"
 PRIMARY_BASE_URL = "http://localhost:11434/v1"
 # Ollama ignores the key; the openai SDK requires a non-empty string.
 PRIMARY_API_KEY = "ollama"
+PRIMARY_KEEP_ALIVE = 90  # seconds; passed as extra_body to OpenAI-compat endpoint
 
 # ── Fallback: OpenRouter Kimi K2.6 (P61/MOL-268) ─────────────────────
 FALLBACK_MODEL = "moonshotai/kimi-k2.6"
@@ -32,8 +41,9 @@
 MAX_TOKENS = 4096
 TEMPERATURE = 0.3
 
-# Hard char budget — see MOL-168. Pro 3.1 has 1M context; qwen3:8b has
-# 32k. The 300k cap is a cost guard for fallback, not a qwen overflow guard.
+# Hard char budget — see MOL-168. Kimi K2.6 fallback has 200k context;
+# qwen3:1.7b has 32k. The 300k cap is a cost guard for fallback, not a
+# qwen overflow guard (qwen would truncate naturally before then).
 MAX_PROMPT_CHARS = 300_000
 
 # qwen3 soft-switch — belt+suspenders; thinking is on by default.
@@ -41,8 +51,11 @@
 
 
 def _call_primary(prompt: str) -> Optional[str]:
-    """Call qwen3:8b via local Ollama OpenAI-compat endpoint.
+    """Call qwen3:1.7b via local Ollama OpenAI-compat endpoint.
 
+    P151/MOL-502: model downsized from qwen3:8b. keep_alive passed via
+    extra_body — Ollama's OpenAI-compat endpoint honors it.
+
     Raises on any error — caller is expected to catch and fall through.
     """
     from openai import OpenAI
@@ -53,12 +66,13 @@
         max_tokens=MAX_TOKENS,
         temperature=TEMPERATURE,
         messages=[{"role": "user", "content": _THINK_PREFIX + prompt}],
+        extra_body={"keep_alive": PRIMARY_KEEP_ALIVE},
     )
     return response.choices[0].message.content
 
 
 def _call_fallback(prompt: str) -> Optional[str]:
-    """Call Pro 3.1 via OpenRouter with reasoning: high."""
+    """Call Kimi K2.6 via OpenRouter with reasoning: high."""
     from openai import OpenAI
 
     api_key = os.environ.get(FALLBACK_API_KEY_ENV, "").strip()
@@ -82,7 +96,7 @@
 
 
 def llm_compose(prompt: str, context: str) -> Optional[str]:
-    """Compose memory via local Qwen primary; fall back to Pro 3.1 on failure.
+    """Compose memory via local Qwen primary; fall back to Kimi K2.6 on failure.
 
     Returns text or None on total failure.
     """
@@ -100,7 +114,7 @@
     try:
         result = _call_primary(full)
         if result:
-            logger.info("memory LLM: qwen3:8b (local, primary)")
+            logger.info("memory LLM: %s (local, primary)", PRIMARY_MODEL)
             return result
         logger.warning("memory LLM primary returned empty content; falling back")
     except Exception as e:
--- a/plugins/memory/tiered/store.py
+++ b/plugins/memory/tiered/store.py
@@ -83,9 +83,11 @@
 
 CREATE VIRTUAL TABLE IF NOT EXISTS memory_vec USING vec0(
   entry_rowid integer primary key,
-  embedding float[1024] distance_metric=cosine,
+  embedding float[768] distance_metric=cosine,
   category text
 );
+-- P151/MOL-502: dim was 1024 (bge-m3); now 768 (nomic-embed-text).
+-- Existing DBs auto-migrate via _migrate_vec_dims() in __init__.
 
 CREATE TABLE IF NOT EXISTS memory_stats (
   source      TEXT PRIMARY KEY DEFAULT 'hermes',
